See [AGENTS.md](AGENTS.md).

That file is deliberately tool-neutral, since contributors here use a range of AI assistants and
the advice is the same for all of them. This file exists only so Claude Code finds it.
