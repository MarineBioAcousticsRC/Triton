function bw_init_batch_figure

global REMORA

defaultPos = [0.25,0.25,0.3,0.6];
REMORA.fig.bw.batch = figure( ...
    'NumberTitle','off', ...
    'Name','Blue whale Detector Batch - v1.0',...
    'Units','normalized',...
    'MenuBar','none',...
    'Position',defaultPos, ...
    'Visible', 'on');

% Detection Settings pulldown
REMORA.bw.menuBatch = uimenu(REMORA.fig.bw.batch,'Label','&Load Settings',...
    'Enable','on','Visible','on');

% Spectrogram load/save params
uimenu(REMORA.bw.menuBatch,'Label','&Select Detector Settings File',...
    'Callback','bw_gui_pulldown(''settingsLoadBatch'')');
