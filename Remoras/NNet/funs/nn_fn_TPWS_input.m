

for iFile = 1:nTPWS
    load(TPWSFile)
    load(IDFile)
    for iID = 1:length(uID)
        % find bouts
        
        % grab data from each bout
        spDataTrain = % max number to grab per file for train
        spDataTest = % max number to grab per file for train
        labelsTrain
        labelsTest
    end
end  